import 'package:flutter/material.dart' show Icon, Icons;

abstract class IconConstants {
  static const Icon comment = Icon(Icons.comment_rounded);
  static const Icon save = Icon(Icons.save_alt_rounded);
}